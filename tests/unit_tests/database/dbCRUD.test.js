// dbCRUD.test.js
"use strict";

/**
 * IndexedDB needs to run in a browser!
 * Get coverage info from Puppeteer.
 * DB creation tasks must be nicely separated. 
 * Refac to have object store, index, transaction funs.
 */

describe("IndexedDB Tests.", () => {
    test("Placeholder", () => {
      expect(1+1).toBe(2);
    });
  });